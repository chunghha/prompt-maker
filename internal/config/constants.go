package config

const (
	// DefaultModel is the model used by the web interface or as a fallback.
	DefaultModel = "gemini-2.5-flash"

	// DefaultModelTemperature is the standard creativity/randomness setting for the AI model.
	DefaultModelTemperature = 0.0
)
